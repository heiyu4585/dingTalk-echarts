
<swiper-item a:if="{{style}}" style="{{style}}" key="tabs-pane-{{key}}">
  <slot></slot>
</swiper-item>

<view a:else class="dtm-tabs-pane-wrap" style="{{tabId !== '' && activeTab !== '' ? tabId === activeTab ? '' : 'height: 0px;' : ''}}"  key="tabs-pane-{{key}}">
  <slot></slot>
</view>
