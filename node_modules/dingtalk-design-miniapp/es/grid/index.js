"use strict";

Component({
  mixins: [],
  data: {},
  props: {
    className: '',
    style: '',
    columnNum: 3,
    layout: 'fixed',
    // 布局方式： flow（流动） || fixed（固定）
    list: [],
    onDmTap: null,
    onTap: null
  },
  didMount: function didMount() {},
  methods: {
    onItemTap: function onItemTap(e) {
      var _this$props = this.props,
        onTap = _this$props.onTap,
        onDmTap = _this$props.onDmTap;
      if (onTap) {
        onTap(e);
      } else if (onDmTap) {
        onDmTap(e, this);
      }
    }
  }
});