"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;
// import colorsConfig from 'dingtalk-theme/colorsConfig-dingtalk-x.json';
var _default = function _default() {
  return [];
  // const radiusKeyMap = Object.keys(colorsConfig.mob.normalizations).filter(item => item.includes('radius'));
  // const radiusMap = radiusKeyMap.map(item => colorsConfig.mob.normalizations[item]);

  // return Object.keys(radiusMap).map(item => ({
  //   title: radiusKeyMap[item],
  //   description: radiusMap[item].description,
  //   className: radiusKeyMap[item],
  //   value: radiusMap[item].value
  // }));
};
exports["default"] = _default;