"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = void 0;
// import colorsConfig from 'dingtalk-theme/colorsConfig-dingtalk-x.json';
var _default = function _default() {
  return [];
  // const shadowKeyMap = Object.keys(colorsConfig.mob.normalizations).filter(item => item.includes('shadow'));
  // const shadowMap = shadowKeyMap.map(item => colorsConfig.mob.normalizations[item]);

  // return Object.keys(shadowMap).map(item => ({
  //   title: shadowKeyMap[item],
  //   description: shadowMap[item].description,
  //   className: shadowKeyMap[item],
  //   style: shadowMap[item].style,
  //   value: shadowMap[item].style['box-shadow']
  // }));
};
exports["default"] = _default;