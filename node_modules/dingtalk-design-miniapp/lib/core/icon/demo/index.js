"use strict";

var _ddiconfont = _interopRequireDefault(require("./ddiconfont"));
var _custom = _interopRequireDefault(require("./custom"));
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }
Page({
  data: {
    showSize: [96, 64, 48, 32],
    presetSize: ['small', 'medium', 'large', 'huge'],
    randomIconType: 'details',
    iconList: [],
    myIconList: []
  },
  onLoad: function onLoad() {
    //  系统自带的 Icon
    var iconList = [];
    {
      Object.keys(_ddiconfont["default"]).forEach(function (name) {
        iconList.push({
          name: name,
          code: _ddiconfont["default"][name]
        });
      });
      var total = iconList.length;
      var padEnd = total % 4 === 0 ? 0 : 4 - total % 4;
      iconList = iconList.concat(new Array(padEnd).fill({
        name: '',
        code: ''
      }));
    }
    this.setData({
      iconList: iconList
    });
  },
  showSize: function showSize(e) {
    my.alert({
      content: "<dd-icon type=\"icon_home_outlined\" size=\"".concat(e.target.dataset.size, "\"/>")
    });
  },
  showName: function showName(e) {
    my.alert({
      content: "<dd-icon type=\"".concat(e.target.dataset.name, "\"/>")
    });
  },
  randomIcon: function randomIcon() {
    var _iconResource$get = _custom["default"].get(),
      type2font = _iconResource$get.type2font;
    var typeArr = Object.keys(type2font);
    var rand = Math.ceil(typeArr.length * Math.random()) - 1;
    var randomIconType = typeArr[rand];
    this.setData({
      randomIconType: randomIconType
    });
  },
  toHome: function toHome() {
    my.navigateTo({
      url: '/src/demo/index/index'
    });
  }
});