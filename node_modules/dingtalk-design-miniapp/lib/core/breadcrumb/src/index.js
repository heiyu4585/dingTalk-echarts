"use strict";

Component({
  mixins: [],
  data: {
    $uiName: 'breadcrumb',
    onItemTap: function onItemTap() {}
  },
  props: {
    style: '',
    className: '',
    breadcrumb: [],
    // onDmCrumbsTap() {}, // 已废弃
    onTap: null
  },
  didMount: function didMount() {},
  didUpdate: function didUpdate() {},
  didUnmount: function didUnmount() {},
  methods: {
    onItemTap: function onItemTap(e) {
      var _this$props = this.props,
        onTap = _this$props.onTap,
        breadcrumb = _this$props.breadcrumb,
        onDmCrumbsTap = _this$props.onDmCrumbsTap;
      var index = e.target.dataset.index;
      if (index === breadcrumb.length - 1) {
        return;
      }
      if (onTap) {
        onTap(e, index);
        return;
      }
      onDmCrumbsTap && onDmCrumbsTap(e, index);
    } // onDmTap(e) {
    //   const { index } = e.target.dataset;
    //   // 最后一项即当前页面，没有导航
    //   if (index !== this.props.breadcrumb.length - 1) {
    //     this.props.onDmCrumbsTap(e, this, index);
    //   }
    // }
  }
});