"use strict";

Component({
  mixins: [],
  data: {
    $uiName: 'segmented-control'
  },
  props: {
    itemClassName: '',
    style: '',
    className: '',
    texts: [],
    active: 0,
    disabled: false,
    onTap: null
  },
  didMount: function didMount() {},
  didUpdate: function didUpdate() {},
  didUnmount: function didUnmount() {},
  methods: {
    onItemTap: function onItemTap(e) {
      var index = e && e.target && e.target.dataset && e.target.dataset.index;
      var _this$props = this.props,
        disabled = _this$props.disabled,
        onTap = _this$props.onTap,
        onDmDisabledItemTap = _this$props.onDmDisabledItemTap,
        onDmItemTap = _this$props.onDmItemTap;
      if (onTap) {
        if (!disabled) {
          onTap(index);
        }
        return;
      }
      disabled ? onDmDisabledItemTap && onDmDisabledItemTap(index) : onDmItemTap && onDmItemTap(index);
    }
  }
});