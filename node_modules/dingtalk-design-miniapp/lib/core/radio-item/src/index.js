"use strict";

Component({
  mixins: [],
  data: {
    $uiName: 'radio-item'
  },
  props: {
    className: '',
    style: '',
    checked: false,
    disabled: false,
    arrowType: '',
    arrowSize: '',
    itemName: '',
    hasArrowIcon: false,
    // 是否有右侧图标
    index: 0,
    data: null,
    onChange: null,
    onDmChange: null,
    onExtraTap: null,
    onDmExtraClick: null
  },
  didMount: function didMount() {},
  didUpdate: function didUpdate() {},
  didUnmount: function didUnmount() {},
  methods: {
    onItemChange: function onItemChange() {
      var _this$props = this.props,
        onDmChange = _this$props.onDmChange,
        onChange = _this$props.onChange,
        index = _this$props.index,
        data = _this$props.data,
        isChecked = _this$props.isChecked,
        checked = _this$props.checked;
      if (onChange) {
        onChange(index);
      } else if (onDmChange) {
        onDmChange(data, index, this, isChecked);
      }
    },
    onItemExtraTap: function onItemExtraTap() {
      var _this$props2 = this.props,
        onDmExtraClick = _this$props2.onDmExtraClick,
        onExtraTap = _this$props2.onExtraTap,
        data = _this$props2.data,
        index = _this$props2.index,
        isChecked = _this$props2.isChecked;
      if (onExtraTap) {
        onExtraTap(index);
      } else if (onDmExtraClick) {
        onDmExtraClick(data, index, this, isChecked);
      }
    }
  }
});