"use strict";

Component({
  mixins: [],
  data: {
    $uiName: 'checkbox-item'
  },
  props: {
    className: '',
    arrowType: '',
    arrowSize: '',
    checked: null,
    isChecked: null,
    // 是否选中状态，默认不选中
    itemName: '单行列表',
    disabled: false,
    hasArrowIcon: false,
    // 是否有右侧图标
    index: 0,
    data: null,
    onChange: null,
    onExtraTap: null,
    onDmChange: null,
    onDmExtraClick: null
  },
  didMount: function didMount() {},
  didUpdate: function didUpdate() {},
  didUnmount: function didUnmount() {},
  methods: {
    onItemChange: function onItemChange(res) {
      var _this$props = this.props,
        onChange = _this$props.onChange,
        onDmChange = _this$props.onDmChange,
        data = _this$props.data,
        index = _this$props.index;
      if (onChange) {
        onChange(index);
      } else if (onDmChange) {
        onDmChange(data, index, this, res.status);
      }
    },
    onItemExtraTap: function onItemExtraTap(id, e, com) {
      var _this$props2 = this.props,
        onDmExtraClick = _this$props2.onDmExtraClick,
        onExtraTap = _this$props2.onExtraTap,
        data = _this$props2.data,
        index = _this$props2.index;
      if (onExtraTap) {
        onExtraTap(index);
      } else if (onDmExtraClick) {
        onDmExtraClick(data, index, this, com.selected);
      }
    }
  }
});